# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Editor directories
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# If you ever add a build step
node_modules/
dist/
build/
*.zip

# Never commit secrets
# (API key is stored in chrome.storage.local, not in files — but just in case)
.env
*.env.*
secrets.json
